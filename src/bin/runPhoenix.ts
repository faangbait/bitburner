import {NS} from "Bitburner";
import {TermLogger} from "/lib/Helpers";

/** @param {NS} ns **/
export async function main(ns: NS) {
    const logger = new TermLogger(ns);

    logger.log("\tPhoenix rises from the ashes...");
    

}
